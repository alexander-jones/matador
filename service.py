
#TODO :actually implement quote service based on alphavantage
class QuoteService:


	def get_endpoints(symbol, start, end):
		return (0.0, 1.0, 0.0) 

	def get_quotes(symbol, start, end):
		return (0.0, 1.0, 0.0) 